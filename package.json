{
  "name": "pbgc-terminated-db-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/engine/*"
  ],
  "scripts": {
    "dev": "npm --workspace @pbgc/web run dev",
    "build": "npm --workspace @pbgc/web run build",
    "test": "vitest run",
    "lint": "tsc --noEmit --pretty false"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "@vitejs/plugin-basic-ssl": "^1.1.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "sql.js": "^1.12.0"
  }
}
